# Component Based Software Engineering Exam Project: AsteroidsFx

To run project:
```
mvn clean install
then
mvn exec:exec
```

```
mvn -pl ScoringSystem spring-boot:run
```
Doesnt work so i changed it to save locally instead
Also Create a folder called "plugins" if you want to use it. I dont know why it doesnt get pushed, it isnt in the .gitignore file.
